{
  "version": 2,
  "buildCommand": "pip install -r requirements.txt --no-cache",
  "installCommand": "pip install -r requirements.txt --no-cache",
  "devCommand": "uvicorn app.main:app --host 0.0.0.0 --port 3001",
  "framework": null
}
